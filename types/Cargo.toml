[package]
name = "hardtrust-types"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
k256 = { version = "0.13", features = ["ecdsa"] }
alloy-primitives = "1"

[dev-dependencies]
serde_json = "1"
hex = "0.4"
